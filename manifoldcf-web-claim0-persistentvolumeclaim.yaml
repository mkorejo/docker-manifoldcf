apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: manifoldcf-web-claim0
  name: manifoldcf-web-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
